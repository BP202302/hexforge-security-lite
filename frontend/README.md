# Frontend

The current Lite UI is served from `website/` as a simple product-style interface with Home, Scanner and Results pages.

This directory is reserved for future UI organization while keeping Lite lightweight.
